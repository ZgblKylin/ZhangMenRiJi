{
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  },
  "scripts": {
    "setup": "npm install && cd frontend && npm install",
    "dev": "npx tauri dev",
    "build": "npm --prefix frontend run build",
    "build:desktop": "npx tauri build",
    "web-backend": "cd backend && cargo run --release",
    "web-backend-debug": "cd backend && cargo run",
    "web-frontend": "cd frontend && npm run dev"
  }
}
